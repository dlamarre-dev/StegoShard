## API Report File for "stegoshard"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export type ApiErrorCode =
/** An output path exists and the caller did not pass `force`. */
'OUTPUT_EXISTS'
/** `keyMode: 'stego'` without a cover image to hide the key in. */
| 'STEGO_NEEDS_COVER'
/** Duress mode without the decoy payload that opens under the second password. */
| 'DURESS_DECOY_REQUIRED'
/** Duress mode without the second, independent password. */
| 'DURESS_PASSWORD_REQUIRED'
/** Non-possession mode without a `k`-of-`n` threshold. */
| 'THRESHOLD_REQUIRED'
/** An access mode was asked for on a path that cannot carry one. */
| 'MODE_NEEDS_DISGUISE'
/** A save was handed no readable input files. */
| 'NO_INPUT_FILES'
/** A restore found no image it could decode among its inputs. */
| 'NO_READABLE_IMAGES'
/** A gallery save found no usable cover photos. */
| 'NO_COVERS_FOUND'
/** A gallery restore was handed no images to scan. */
| 'NO_GALLERY_IMAGES';

// @public
export type ApiErrorParams = Record<string, string | number>;

// @public
export interface Argon2Params {
    // (undocumented)
    iterations: number;
    // (undocumented)
    memoryKiB: number;
    // (undocumented)
    parallelism: number;
}

// @public (undocumented)
export const BINARY_VERSION = 1;

// @public
export function binaryExtension(variant: BinaryVariant): string;

// @public (undocumented)
export function binaryKeyName(variant: BinaryVariant): string;

// @public
export type BinaryVariant = 'branded' | 'disguised';

// @public
export function binaryVaultName(variant: BinaryVariant): string;

// @public (undocumented)
export interface BrandBandInput {
    lines?: readonly string[];
    recovery: readonly string[];
}

// @public
export function brandCaption(input: BrandCaptionInput): {
    lines: string[];
    unstampableTitle: string | null;
};

// @public
export interface BrandCaptionInput {
    date?: string | undefined;
    index?: number | undefined;
    title?: string | undefined;
    // (undocumented)
    total?: number | undefined;
}

// @public
export class BucketTooLargeError extends Error {
    constructor(needed: number, limit: number);
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    readonly needed: number;
}

// @public
export function buildDuressDbContainer(realFilename: string, realContent: Uint8Array, decoyFilename: string, decoyContent: Uint8Array, realPassword: string, duressPassword: string, keyFactor?: Uint8Array | null, params?: Argon2Params, onProgress?: OnProgress, chunkSize?: number, bundle?: boolean): Promise<{
    container: Uint8Array;
}>;

// @public
export function buildNonPossessionDbContainer(filename: string, content: Uint8Array, password: string, k: number, n: number, keyFactor?: Uint8Array | null, params?: Argon2Params, onProgress?: OnProgress, chunkSize?: number, bundle?: boolean): Promise<{
    container: Uint8Array;
    shares: Uint8Array[];
}>;

// @public
export function clearUserEntropy(): void;

// @public (undocumented)
export interface Codec {
    capacity(profile: number): number;
    // (undocumented)
    decode(image: ImageDataLike): Uint8Array;
    // (undocumented)
    encode(payload: Uint8Array, profile: number): ImageDataLike;
    readonly id: number;
}

// @public
export const CODEC_COLOR_GRID = 2;

// @public (undocumented)
export const CODEC_QR_GRID = 0;

// @public
export function codecName(id: number): string;

// @public
export function createKeyBlock(password: string, params?: Argon2Params): Promise<{
    dek: CryptoKey;
    block: KeyBlock;
}>;

// @public
export function createVaultKey(password: string, params?: Argon2Params): Promise<VaultKey>;

// @public (undocumented)
export interface CredentialCheck {
    // (undocumented)
    ok: boolean;
    // (undocumented)
    reason?: CredentialRelation;
}

// @public
export type CredentialRelation = 'equal' | 'case' | 'contains' | 'reverse' | 'near';

// @public
export function credentialsIndependent(real: string, duress: string): CredentialCheck;

// @public
export class CredentialsNotIndependentError extends Error {
    constructor(reason: CredentialRelation);
    // (undocumented)
    readonly reason: CredentialRelation;
}

// @public (undocumented)
export function decodeHeader(bytes: Uint8Array): Header;

// @public
export function decodeShareText(text: string): Uint8Array;

// @public
export function decodeWithAnyCodec(image: ImageDataLike): Uint8Array;

// @public
export const DEFAULT_ARGON2: Argon2Params;

// @public
export function drawBrandBand(img: ImageDataLike, band: BrandBandInput): ImageDataLike;

// @public
export function embedKeyBlockStego(rgba: Uint8Array | Uint8ClampedArray, width: number, height: number, keyBlock: Uint8Array, password: string, params?: Argon2Params): Promise<void>;

// @public
export function embedKeyBlockStegoJpeg(jpegBytes: Uint8Array, keyBlock: Uint8Array, password: string, params?: Argon2Params): Promise<Uint8Array>;

// @public
export function embedKeyFactorStego(rgba: Uint8Array | Uint8ClampedArray, width: number, height: number, factor: Uint8Array, password: string, params?: Argon2Params): Promise<void>;

// @public
export function embedKeyFactorStegoJpeg(jpegBytes: Uint8Array, factor: Uint8Array, password: string, params?: Argon2Params): Promise<Uint8Array>;

// @public
export function encodeShareText(share: Uint8Array): string;

// @public
export function estimateGalleryCovers(filename: string, content: Uint8Array, keyMode?: KeyMode): Promise<{
    k: number;
    m: number;
    needed: number;
}>;

// @public
export function estimateImages(filename: string, content: Uint8Array, options?: ExportOptions): Promise<{
    k: number;
    m: number;
    images: number;
}>;

// @public (undocumented)
export interface ExportOptions {
    bundle?: boolean | undefined;
    // (undocumented)
    codecId?: number | undefined;
    identity?: VaultIdentity | undefined;
    keyMode?: KeyMode | undefined;
    // (undocumented)
    profile?: number | undefined;
}

// @public (undocumented)
export interface ExportResult {
    imagePayloads: Uint8Array[];
    // (undocumented)
    k: number;
    keyBlock: Uint8Array;
    // (undocumented)
    keyMode: KeyMode;
    // (undocumented)
    m: number;
    // (undocumented)
    setId: Uint8Array;
}

// @public (undocumented)
export function exportVault(filename: string, content: Uint8Array, key: VaultKey, options?: ExportOptions): Promise<ExportResult>;

// @public
export function exportVaultBinary(filename: string, content: Uint8Array, key: VaultKey, options?: {
    keyMode?: KeyMode;
    variant?: BinaryVariant;
    maxBytes?: number;
    bundle?: boolean | undefined;
    identity?: VaultIdentity | undefined;
}, onProgress?: OnProgress): Promise<{
    container: Uint8Array;
    keyMode: KeyMode;
    keyBlock: Uint8Array;
}>;

// @public
export function exportVaultBinaryDisguised(filename: string, content: Uint8Array, password: string, options?: {
    keyMode?: KeyMode;
    maxBytes?: number;
    bundle?: boolean | undefined;
}, onProgress?: OnProgress): Promise<{
    container: Uint8Array;
    keyMode: KeyMode;
    keyBlock: Uint8Array;
    regionIndex: number;
    dek: Uint8Array;
}>;

// @public
export function extractKeyBlockStego(rgba: Uint8Array | Uint8ClampedArray, width: number, height: number, password: string, params?: Argon2Params): Promise<Uint8Array | null>;

// @public
export function extractKeyBlockStegoJpeg(jpegBytes: Uint8Array, password: string, params?: Argon2Params): Promise<Uint8Array | null>;

// @public
export function extractKeyFactorStego(rgba: Uint8Array | Uint8ClampedArray, width: number, height: number, password: string, params?: Argon2Params): Promise<Uint8Array | null>;

// @public
export function extractKeyFactorStegoJpeg(jpegBytes: Uint8Array, password: string, params?: Argon2Params): Promise<Uint8Array | null>;

// @public
export type FilePurpose =
/** Holds the secret: the .db/.ssbn container, or the encoded image set. */
'vault'
/** Every image bundled into one .zip for convenience. */
| 'archive'
/** The printable paper sheet. */
| 'document'
/** Gallery fragment photos. */
| 'photos'
/** A key archived separately from the vault. */
| 'keyfile'
/** An ordinary-looking photo carrying the hidden key. */
| 'stegoCover'
/** One holder's Shamir threshold share. */
| 'share';

// @public
export class FileTooLargeError extends Error {
    constructor(size: number, limit: number);
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    readonly size: number;
}

// @public (undocumented)
export const FORMAT_VERSION = 2;

// @public
export function fromBase64(b64: string): Uint8Array;

// @public
export const GALLERY_MAX_IMAGES = 256;

// @public
export const GALLERY_MIN_DECOYS = 2;

// @public
export const GALLERY_MIN_IMAGES = 5;

// @public
export type GalleryCover = {
    kind: 'jpeg';
    name: string;
    jpeg: Uint8Array;
} | {
    kind: 'rgba';
    name: string;
    rgba: Uint8Array | Uint8ClampedArray;
    width: number;
    height: number;
};

// @public
export class GalleryCoverCapacityError extends Error {
    constructor(coverName: string, capacityBits: number, neededBits: number);
    // (undocumented)
    readonly capacityBits: number;
    // (undocumented)
    readonly coverName: string;
    // (undocumented)
    readonly neededBits: number;
}

// @public
export function galleryDecode(images: GalleryCover[], password: string, options?: GalleryDecodeOptions): Promise<{
    filename: string;
    content: Uint8Array;
    bundled: boolean;
}>;

// @public (undocumented)
export interface GalleryDecodeOptions {
    keyBlock?: Uint8Array | undefined;
    // (undocumented)
    params?: Argon2Params;
    secret?: Uint8Array | undefined;
}

// @public
export function galleryEncode(filename: string, content: Uint8Array, password: string, covers: GalleryCover[], options?: GalleryEncodeOptions): Promise<GalleryEncodeResult>;

// @public (undocumented)
export interface GalleryEncodeOptions {
    bundle?: boolean | undefined;
    keyMode?: KeyMode;
    mode?: 'plain' | 'nonpossession';
    params?: Argon2Params;
    // (undocumented)
    threshold?: {
        k: number;
        n: number;
    } | undefined;
}

// @public (undocumented)
export interface GalleryEncodeResult {
    // (undocumented)
    decoys: number;
    // (undocumented)
    images: GalleryImage[];
    // (undocumented)
    k: number;
    keyBlock: Uint8Array;
    // (undocumented)
    m: number;
    // (undocumented)
    setId: Uint8Array;
    shares?: Uint8Array[] | undefined;
}

// @public
export class GalleryFileTooLargeError extends Error {
    constructor(size: number, limit: number);
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    readonly size: number;
}

// @public
export type GalleryImage = {
    kind: 'jpeg';
    name: string;
    jpeg: Uint8Array;
} | {
    kind: 'rgba';
    name: string;
    rgba: Uint8Array;
    width: number;
    height: number;
};

// @public
export class GalleryRestoreError extends Error {
    constructor();
}

// @public
export class GalleryTooFewImagesError extends Error {
    constructor(provided: number, needed: number);
    // (undocumented)
    readonly needed: number;
    // (undocumented)
    readonly provided: number;
}

// @public
export class GalleryTooManyImagesError extends Error {
    constructor(provided: number, limit: number);
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    readonly provided: number;
}

// @public
export function getCodec(id: number): Codec;

// @public
export function hasUserEntropy(): boolean;

// @public (undocumented)
export interface Header {
    // (undocumented)
    blobLen: number;
    // (undocumented)
    codecId: number;
    // (undocumented)
    hash: Uint8Array;
    // (undocumented)
    k: number;
    // (undocumented)
    m: number;
    // (undocumented)
    profile: number;
    // (undocumented)
    setId: Uint8Array;
    // (undocumented)
    shardIndex: number;
    // (undocumented)
    shardLen: number;
    // (undocumented)
    version: number;
}

// @public
export interface ImageDataLike {
    // (undocumented)
    data: Uint8ClampedArray;
    // (undocumented)
    height: number;
    // (undocumented)
    width: number;
}

// @public
export function importVault(payloads: Uint8Array[], password: string, opts?: {
    keyBlock?: Uint8Array | undefined;
}): Promise<{
    filename: string;
    content: Uint8Array;
    bundled: boolean;
    identity?: VaultIdentity | undefined;
}>;

// @public
export function importVaultBinary(container: Uint8Array, password: string, opts?: {
    keyBlock?: Uint8Array | undefined;
    maxBytes?: number;
    secret?: Uint8Array | null;
}, onProgress?: OnProgress): Promise<{
    filename: string;
    content: Uint8Array;
    bundled: boolean;
    identity?: VaultIdentity | undefined;
}>;

// @public
export function installUserEntropy(text: string): Promise<void>;

// @public
export function isJpeg(bytes: Uint8Array): boolean;

// @public
export function isSerializedKeyBlock(bytes: Uint8Array): boolean;

// @public
export function jpegStegoCapacityBits(jpegBytes: Uint8Array): number;

// @public
export class JpegUnsupportedError extends Error {
    constructor(reason: string);
}

// @public
export const KEY_BLOCK_LEN: number;

// @public
export const KEY_FACTOR_LEN = 32;

// @public (undocumented)
export interface KeyBlock {
    // (undocumented)
    iv: Uint8Array;
    // (undocumented)
    params: Argon2Params;
    // (undocumented)
    salt: Uint8Array;
    // (undocumented)
    wrapped: Uint8Array;
}

// @public
export type KeyMode = 'embedded' | 'keyfile' | 'stego';

// @public
export function looksLikeBinaryContainer(head: Uint8Array): boolean;

// @public
export interface ManifestEntry {
    // (undocumented)
    name: string;
    // (undocumented)
    purpose: FilePurpose;
}

// @public
export const MAX_FILE_BYTES: number;

// @public
export const MAX_FILE_BYTES_BINARY_CLI: number;

// @public (undocumented)
export const MAX_FILE_BYTES_BINARY_UI: number;

// @public
export const MAX_IMAGES = 150;

// @public
export class MissingKeyError extends Error {
    constructor();
}

// @public
export function normalizePassword(password: string): string;

// @public (undocumented)
export type OnProgress = (p: Progress) => void;

// @public
export function parseKeyBlock(bytes: Uint8Array): KeyBlock;

// @public (undocumented)
export const PROFILE_CLOUD = 1;

// @public (undocumented)
export const PROFILE_DISK = 0;

// @public (undocumented)
export const PROFILE_PAPER = 2;

// @public
export interface Progress {
    done: number;
    phase: 'compress' | 'encrypt' | 'decrypt' | 'verify' | 'unlock' | 'render';
    total: number;
}

// @public
export function recoveryLines(codecName: string): string[];

// @public (undocumented)
export const SECRET_LEN = 32;

// @public
export class SegmentedFormatError extends Error {
    constructor(message: string);
}

// @public
export function serializeKeyBlock(block: KeyBlock): Uint8Array;

// @public
export function shamirRecover(shares: Uint8Array[]): Promise<Uint8Array>;

// @public
export function shamirSplit(secret: Uint8Array, k: number, n: number): Promise<Uint8Array[]>;

// @public
export const SHARE_LEN: number;

// @public
export class ShareChecksumError extends Error {
    constructor();
}

// @public
export function shareFileText(share: Uint8Array, index: number, n: number, k: number, loadHint: string, style?: ShareTextStyle): string;

// @public
export class ShareSetError extends Error {
    constructor(message: string);
}

// @public
export type ShareTextStyle = 'branded' | 'neutral';

// @public
export const STEGO_ERROR_CODES: readonly StegoErrorCode[];

// @public
export class StegoCapacityError extends Error {
    constructor(capacityBits: number);
    // (undocumented)
    readonly capacityBits: number;
}

// @public
export class StegoCoverFormatError extends Error {
    constructor();
}

// @public
export type StegoErrorCode = 'BUCKET_TOO_LARGE' | 'CREDENTIALS_NOT_INDEPENDENT' | 'FILE_TOO_LARGE' | 'GALLERY_COVER_CAPACITY' | 'GALLERY_FILE_TOO_LARGE' | 'GALLERY_RESTORE_FAILED' | 'GALLERY_TOO_FEW_IMAGES' | 'GALLERY_TOO_MANY_IMAGES' | 'JPEG_UNSUPPORTED' | 'MISSING_KEY' | 'SEGMENTED_FORMAT' | 'SHARE_CHECKSUM' | 'SHARE_SET' | 'STEGO_CAPACITY' | 'STEGO_COVER_FORMAT' | 'TOO_MANY_FILES' | 'TOO_MANY_IMAGES' | 'VERIFICATION_FAILED' | 'WRONG_PASSWORD';

// @public
export function stegoErrorCode(err: unknown): StegoErrorCode | null;

// @public
export function stegoErrorDetails(err: unknown): Record<string, string | number> | undefined;

// @public
export class StegoShardApiError extends Error {
    constructor(code: ApiErrorCode, message: string, params?: ApiErrorParams | undefined);
    // (undocumented)
    readonly code: ApiErrorCode;
    // (undocumented)
    readonly params?: ApiErrorParams | undefined;
}

// @public
export function toBase64(bytes: Uint8Array): string;

// @public
export function toHex(bytes: Uint8Array): string;

// @public
export class TooManyFilesError extends Error {
    constructor(count: number, limit: number);
    // (undocumented)
    readonly count: number;
    // (undocumented)
    readonly limit: number;
}

// @public
export class TooManyImagesError extends Error {
    constructor(count: number, limit: number);
    // (undocumented)
    readonly count: number;
    // (undocumented)
    readonly limit: number;
}

// @public
export function unlockKeyBlock(block: KeyBlock, password: string): Promise<CryptoKey>;

// @public
export function unwrapBinary(bytes: Uint8Array): {
    payload: Uint8Array;
    variant: BinaryVariant;
} | null;

// @public
export interface VaultIdentity {
    sequence: number;
    vaultId: Uint8Array;
}

// @public
export interface VaultKey {
    // (undocumented)
    dek: CryptoKey;
    // (undocumented)
    keyBlock: Uint8Array;
}

// @public
export class VerificationError extends Error {
    constructor();
}

// @public
export function verifyBinaryExport(container: Uint8Array, dek: CryptoKey, filename: string, content: Uint8Array, onProgress?: OnProgress): Promise<void>;

// @public
export function verifyDisguisedExport(container: Uint8Array, dek: Uint8Array, regionIndex: number, filename: string, content: Uint8Array, onProgress?: OnProgress): Promise<void>;

// @public
export function verifyGalleryExport(images: GalleryImage[], password: string, keyBlock: Uint8Array | undefined, filename: string, content: Uint8Array, secret?: Uint8Array | undefined): Promise<void>;

// @public
export function verifyImageExport(imagePayloads: Uint8Array[], dek: CryptoKey, filename: string, content: Uint8Array): Promise<void>;

// @public
export const WARN_FILE_BYTES: number;

// @public
export function wrapBinary(payload: Uint8Array, variant: BinaryVariant): Uint8Array;

// @public
export class WrongPasswordError extends Error {
    constructor();
}

// (No @packageDocumentation comment for this package)

```
